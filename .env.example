# Passcode for /admin. CHANGE THIS before the event.
ADMIN_PASSCODE=pizza2026

# Where the SQLite file lives. On a host with a mounted disk, point this at the disk.
DATA_DIR=./data

# Only needed by `npm run poster` — the public URL players will land on.
PUBLIC_URL=https://your-app.example.com
